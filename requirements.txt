# Application dependencies
PyQt5
PyOpenGL
# numpy 2.x dropped 32-bit Windows wheels; pin <2.0 so the x86 build can install it.
numpy<2.0
Pillow

# Build
cx_Freeze

# Testing
pytest
pytest-cov
pytest-qt
